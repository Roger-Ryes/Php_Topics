Hola soy un archivo de texto, para mostrar a la web se hace:
1. llamarlo con una variable php
    ejem $archivo = fopen("direccionDeFichero","permisos");
2. Leer el contenido
    con fgets($archivo);
3. Cerrar el archivo
    fclose($archivo);
Con el comando fwrite escribo en el archivo
Con el comando fwrite escribo en el archivo
Con el comando fwrite escribo en el archivo