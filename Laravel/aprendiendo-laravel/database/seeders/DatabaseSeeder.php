<?php
//SEEDING
//ref: https://laravel.com/docs/8.x/seeding#introduction

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    /**
     * Seed the application's database.
     *
     * @return void
     */
    
    /*run. This method is called when the "db:seed" Artisan command is executed. Within the run method, you may insert data into 
     * your database however you wish*/
    public function run()
    {
        // \App\Models\User::factory(10)->create();
    }
}
